import React from "react";
import background from "../../../src/home.jpg";

const AboutPage = () => (
  <div
    style={{
      backgroundImage: `url(${background})`,
      height: "100vh",
      backgroundSize: "cover"
    }}
  >
  </div>
);

export default AboutPage;
